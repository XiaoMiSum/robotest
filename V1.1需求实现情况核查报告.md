# V1.1 需求实现情况核查报告

> 核查日期：2026-08-04
> 核查基线：`docs/需求/需求规格说明书.md`（V1.1，2026-07-31 起草中）、`docs/需求/AI功能用户故事集.md`（US-AI-001~018）
> 核查分支：`feature/v1.1-ai-agents`（最近提交 `24cd518`，2026-08-04；初版基线 `d4d2235`）
> 核查方式：需求文档逐条比对 + 后端 Java 源码扫描 + 前端 Vue/TS 源码扫描 + Git 提交历史检索 + 数据库 DDL 检查 + AI 资源文件检查
> 复查说明：本版为增量复查，纳入 `85f468e` 之后的 7 个提交（`2367d66` / `8ed8f6b` / `927824f` / `c51ad87` / `fecffdb` / `f80b31a` / `24cd518`），结论更新点以 ⭐ 标注

---

## 一、总体结论

V1.1 需求共 **17 个功能点（16 个用户故事 + AI 基础设施）**，核查结果：

| 状态 | 数量 | 占比 | 说明 |
|---|---|---|---|
| ✅ 已实现 | 17 | 100% | 基础设施 + 用例生成 4 项 + 评审 3 项 + 缺陷 3 项（008/009/010）+ 回归子集推荐（018）+ 执行顺序推荐（017）+ ⭐全局助手 4 项（011/012/013/014） |
| ⚠️ 部分实现 | 0 | 0% | — |
| ❌ 未实现 | 0 | 0% | — |

> 注：需求中 US-AI-015（批量生成）、US-AI-016（DSL 转换）在 V1.1 规格书中已标注移除，不在本次核查范围内；残留物核查见第六节风险项。

**核心结论**：V1.1 的"生成—评审—缺陷—回归推荐—顺序推荐—全局助手"六大能力链已全部落地可用（前后端闭环）。⭐ 相比上版：全局智能助手（3.5）整域已实现——服务端会话管理/Function Calling/写工具确认/脑图指令翻译/平台指引知识库（`2367d66`/`8ed8f6b`/`927824f`），前端会话面板/流式回复/确认卡片/DSL 执行/链接白名单（`c51ad87`），悬浮入口挂载业务布局与核心单测（`fecffdb`/`f80b31a`/`24cd518`）。实施梯队一至四全部达成。

---

## 二、逐项核查明细

### 2.1 AI 基础设施（3.1）— ✅ 已实现

| 需求点 | 状态 | 证据 |
|---|---|---|
| AI 网关与供应商接入 | ✅ | `AiGatewayService`、`OpenAiCompatProvider`、`provider-presets.json`（OpenAI/DeepSeek/通义/智谱/Kimi/兼容模式 6 家） |
| AI 配置管理 | ✅ | `AiConfigService`、`AiChatModelService`、`ProviderPresetRegistry`、`AiSettingDefinitions`、`AiCryptoUtil`（密钥加密存储）；`admin/AiConfigController`、`workspace/AiStatusController` |
| 智能体与提示词 | ✅ | `AiAgentService`、`PromptAssembler`、`prompt-defaults.json`（按功能域组织） |
| 调用审计与统计 | ✅ | `AiAuditRecorder`、`AiInvocationLog`（v1.1.sql 建表，v1.2.sql 修复主键）、`AiStatisticsService` |
| 限流与成本控制 | ✅ | `AiRateLimiter`（每空间/用户/模型维度） |
| 流式输出（SSE） | ✅ | `AiGatewayServiceImpl#stream` → `SseEmitter`；前端 `useAiStream.ts`（含单测） |
| 前端管理界面 | ✅ | `web/src/pages/admin/AiConfigPage.vue`、`AiAgentsPage.vue`、`components/common/AiModelSelect.vue`、`stores/ai.ts`（含 aiEnabled 总开关，关闭时前端隐藏全部 AI 入口） |

### 2.2 智能测试用例生成（3.2）— ✅ 已实现

| 需求点 | 用户故事 | 状态 | 证据 |
|---|---|---|---|
| 一键生成用例子树 | US-AI-001 | ✅ | `AiCaseController`（脑图路由）、`CaseGenerationService`、`minder/ai/AiGeneratePanel.vue`、`AiPreviewTree.vue`、`aiMount.ts` |
| 补全用例步骤 | US-AI-002 | ✅ | `AiCaseController#complete`、`CaseStepCompletionService`、AiGeneratePanel 内步骤补全入口 |
| 用例优先级推荐 | US-AI-003 | ✅ | `AiPriorityService`（规则引擎+LLM 兜底）、AiGeneratePanel 优先级入口 |
| 轻量需求池 | US-AI-004 | ✅ | `RequirementController`、`DocumentRequirementController`、`RequirementPoolPage.vue`、`RequirementSelector.vue`（用例生成时选择需求来源） |
| AI 生成标识 | ✅ | ✅ | `ai_generated` 字段（v1.1.sql 用例表/缺陷表） |

### 2.3 AI 辅助评审与覆盖度（3.3）— ✅ 已实现

| 需求点 | 用户故事 | 状态 | 证据 |
|---|---|---|---|
| AI 一键检查 | US-AI-005 | ✅ | `AiReviewCheckService`、`ReviewAiCheckPanel.vue`、`AiReviewController` |
| 评审摘要 | US-AI-006 | ✅ | `AiReviewSummaryService`、`ReviewAiSummary.vue` |
| 遗漏测试点分析 | US-AI-007 | ✅ | `AiMissingPointService`（关键词匹配版）、`MissingPointsPanel.vue`；语义升级版本存在但未启用 |

### 2.4 缺陷智能分析与去重（3.4）— ✅ 已实现

| 需求点 | 用户故事 | 状态 | 证据与缺口 |
|---|---|---|---|
| 缺陷标题优化与等级建议 | US-AI-008 | ✅ | `BugAiController#suggest`、`BugAiSuggest.vue`、`BugCreatePage.vue` 已接入（⭐ 前端已随 `4cd04b5` 提交，不再滞留工作区） |
| 缺陷语义查重 | US-AI-009 | ✅ | `BugDedupService`、`BugEmbedding`、向量库、`useBugDedup.ts`、`BugDedupList.vue`；⭐ 增量向量写入已接线（`1fc481b`：缺陷创建/更新/关闭时经 `BugServiceImpl` 触发，`BugServiceImplTest` 补充断言），10 分钟补偿任务降级为兜底 |
| 缺陷聚类分析 | US-AI-010 | ✅ | 后端 `BugClusterService`、`BugClusterPanel.vue`、`bugClusterChart.ts` 完整；⭐ 看板已挂载（`4cd04b5`：`BugListPage` 聚类入口 + 建议/查重入口，`components.d.ts` 同步注册） |

### 2.5 全局智能助手（3.5）— ⭐✅ 已实现

| 需求点 | 用户故事 | 状态 | 证据 |
|---|---|---|---|
| 自然语言查询 | US-AI-011 | ⭐✅ | 服务端 `AiAssistantChatController` / `AiAssistantChatServiceImpl`（会话 CRUD + SSE 流式 + 只读工具接线）、`service/ai/assistant/`（`AiTool` 框架 + `QueryCasesTool`/`QueryPlansTool`/`QueryReviewsTool`/`QueryBugsTool` 四只读工具）；前端 `AssistantPanel.vue` 会话列表 + `useAssistantStream.ts` 流式解析（含单测）、`useConversationList.ts` 游标分页（含单测） |
| 快捷操作执行 | US-AI-012 | ⭐✅ | 服务端 `WriteToolExecutor` + `AiConfirmTokenService`（写工具经一次性确认令牌放行）、SSE `confirm_required` 帧；前端 `MessageItem.vue` 确认卡片 + `assistantConfirm.ts` 纯函数（含单测 13 例） |
| 对话式脑图编辑 | US-AI-013 | ⭐✅ | 服务端 `AiMinderTranslationTool`（翻译为 `minder_commands` 帧，经 `PromptAssembler` 组装，`dsl_translation` 模板复用）；前端 `dslRunner.ts` + `DslPreviewDialog.vue`（预览-确认-执行-回滚）+ `CaseMindMap.vue` 页面上下文桥（`assistantContext` store 注入 documentId/selectedNodeId） |
| 平台使用指引 | US-AI-014 | ⭐✅ | 服务端 `GetPlatformGuideTool` + `GuideKnowledgeBase`（8 个 `guide/*.md` 片段按角色过滤）；前端 `AssistantFab.vue` 悬浮入口挂载 `BusinessLayout`（`aiStore.aiEnabled` 显隐、拖拽定位、最小化保持会话）+ `assistantLinkWhitelist.ts` 链接白名单兜底（含单测） |

Git 全历史检索：`2367d66`（会话管理与只读工具）/ `8ed8f6b`（Function Calling 链路、SSE 发送与写工具确认）/ `927824f`（脑图指令翻译与 minder_commands 帧）/ `c51ad87`（前端链路 15 文件）/ `fecffdb`（面板滚动适配）/ `f80b31a`（悬浮入口挂载）/ `24cd518`（核心模块单测）。数据库：`ai_conversation` / `ai_message` 建表于 v1.1.sql，索引 `idx_conv_user_ws`、`idx_msg_conversation_id` 符合 C5/C9。

### 2.6 测试计划与风险评估（3.6）— ⚠️ 部分实现

| 需求点 | 用户故事 | 状态 | 证据 |
|---|---|---|---|
| 执行顺序推荐 | US-AI-017 | ⭐✅ | 后端 `AiPlanOrderRecommendServiceImpl`（`snapshot_synced_at` 失效判定 + 历史缺陷/优先级/模块缺陷密度三因子加权 + LLM 按需生成理由，单测 6 例，提交 `ff1db41`）；前端 `PlanOrderRecommend.vue`（✨ 标签页 + 开始/重新计算 + 70s 超时可取消 + stale 重算提示 + 因子明细 + 行点击定位脑图 + #序号徽标双向联动）、`planOrderRecommend.ts` 纯函数、`PlanDetailPage` 标签页接入（提交 `85f468e`） |
| 回归子集推荐 | US-AI-018 | ⭐✅ | 后端 `AiRegressionRecommendService(Impl)`（模块名精确 1.0/模糊 0.9 + 语义 TopK 阈值 0.7 + 降级关键词 0.6 + both 合并去重 + 理由批量生成 60s 读超时，单测 13 例）、`AiCaseController#POST /plans/regression-recommend`、`AiRegressionRecommendReq/RespDTO`、`TestCaseNodeMapper.listCaseNodesByDocumentIds`（提交 `202703b`）；前端 `RegressionRecommendDialog.vue`（模块/变更说明/需求条目三态输入 + 勾选清单默认全选 + 70s 超时可取消 + 降级提示条「当前为模块名/关键词匹配结果」+ 另存需求池条目）、`services/ai.ts#regressionRecommend`、`PlanDetailPage` 入口 + 带入既有 CaseSelector 流程（提交 `12ca022`） |

---

## 三、实施梯队覆盖情况

对照需求文档"实施梯队"规划：

| 梯队 | 规划内容 | 覆盖情况 |
|---|---|---|
| 梯队一 | AI 基础设施、用例子树生成、步骤补全、评审摘要 | ✅ 全部覆盖 |
| 梯队二 | 缺陷表单建议、评审一键检查、需求池、遗漏测试点（关键词版）、助手使用指引 | ✅ 全部覆盖（⭐助手使用指引：`GetPlatformGuideTool` + 8 个 guide 片段 + 前端链接白名单） |
| 梯队三 | 缺陷语义查重、遗漏测试点（语义版）、回归子集推荐、助手自然语言查询、执行顺序推荐 | ⚠️ 语义查重、⭐回归子集推荐、⭐执行顺序推荐、⭐助手自然语言查询已覆盖；仅遗漏点语义版未启用（关键词版已在用） |
| 梯队四 | 助手快捷操作、对话式脑图编辑、缺陷聚类看板、优先级推荐 | ✅ 全部覆盖（⭐助手快捷操作：写工具确认令牌；⭐对话式脑图编辑：minder_commands 帧 + DSL 执行） |

---

## 四、非功能需求符合情况

| 非功能点 | 状态 | 说明 |
|---|---|---|
| 4.1 性能 | ✅ | SSE 流式、表单内嵌非阻塞交互、向量查重独立线程 |
| 4.2 安全 | ✅ | 密钥 `AiCryptoUtil` 加密、上下文头传递（C4）、Prompt 隔离、审计隔离 |
| 4.3 可用性 | ✅ | AI 标识徽标、加载态/取消、预览-确认-执行（脑图挂载） |
| 4.4 可靠性 | ✅ | AI 开关降级（前端隐藏+后端 503）、失败提示重试、向量失败补偿 |
| 4.5 成本 | ✅ | 限流 + 调用量统计 + 上下文裁剪 |

---

## 五、质量门禁与测试情况

- 后端单测与实现一一对应（生成/评审/缺陷/遗漏点/统计/⭐回归推荐/⭐顺序推荐均有测试类；`AiRegressionRecommendServiceImplTest` 13 例覆盖空输入校验、模块精确/模糊评分、语义 TopK 阈值与降级、both 合并决胜、理由长度不匹配置空、理由调用异常不阻断、50 条截断、另存需求池先保存等分支），已随 `d4d2235`、`1fc481b`、`202703b` 等提交
- 前端单测：`useAiStream.ts`、⭐回归推荐前端；⭐全局助手核心模块单测（`useAssistantStream.spec.ts` 15 例覆盖多事件解析/异常帧容错/会话状态机、`useConversationList.spec.ts` 9 例覆盖游标分页/去重/置顶/空间切换/失败解锁、`assistantConfirm.ts` 13 例、`assistantLinkWhitelist.ts` 13 例），已随 `c51ad87`、`24cd518` 提交
- ⭐ 前端全量门禁通过：`pnpm lint` + `vue-tsc`（typecheck）+ `vitest`（194 用例 / 18 文件全绿）+ `vitest --coverage`（C8，关键模块 useAssistantStream/useConversationList/assistantLinkWhitelist/assistantConfirm 均 100%）
- **未覆盖**：遗漏点语义版未启用故无语义路径单测；⭐服务端助手域（会话服务/工具执行器/确认令牌/翻译工具）暂无专项单测，建议补齐（见第六节风险项 9）
- 报告撰写时未运行 `scripts/validate.sh`（Windows 环境不支持，改用前端三件套等价验证）

---

## 六、遗留问题与风险

1. ~~🔴 缺陷智能前端未提交~~ → ⭐ **已解决**：`4cd04b5` 提交 5 个新文件（`BugAiSuggest.vue`/`BugClusterPanel.vue`/`BugDedupList.vue`/`bugClusterChart.ts`/`useBugDedup.ts`）+ 5 个接线修改（`BugCreatePage`/`BugListPage`/`services/ai.ts`/`services/index.ts`/`types/index.ts`），无丢失风险。
2. ~~🟠 US-AI-009 增量写入缺口~~ → ⭐ **已解决**：`1fc481b` 缺陷创建/更新/关闭时实时触发向量写入（`BugServiceImpl` + `AiEmbeddingWriteServiceImpl`），查重盲区由最长 10 分钟收敛为实时。
3. ~~🟠 US-AI-010 聚类看板未挂载~~ → ⭐ **已解决**：`4cd04b5` 在 `BugListPage` 挂载 `BugClusterPanel`，用户已有入口。
4. ~~**🟡 全局助手整域缺失（3.5）**：V1.1 核心体验项，需排期。~~ → ⭐ **已解决**：服务端会话/工具/翻译/指引（`2367d66`/`8ed8f6b`/`927824f`）+ 前端面板/流式/确认/DSL（`c51ad87`）+ 悬浮入口与单测（`fecffdb`/`f80b31a`/`24cd518`），前后端闭环。
5. ~~**🟡 执行顺序推荐（US-AI-017）仅铺垫**~~ → ⭐ **已解决**：后端评分服务 + 前端入口全部闭环（提交 `ff1db41` / `85f468e`）。
6. **🟡 残留物**：`prompt-defaults.json` 中 `text_import` 模板、`AiCaseController` 的 `/minder/import` 端点疑似对应已移除的 US-AI-015/016，建议清理或确认用途。⭐ `dsl_translation` 模板已确认被 `AiMinderTranslationTool` 复用（US-AI-013），**不再视为残留**。
7. **ℹ️ 文档版本**：需求规格说明书状态仍为"起草中"；本版核查后 17 项功能点已全部实现，建议更新需求文档状态并同步迭代计划。
8. ~~**ℹ️ 工作区遗留**：`AiRateLimiter.java`（`Long allowed` → `long allowed` 空值语义调整）与 `AiPlanOrderScoring.java`（`isStale` 方法位置调整）存在未提交改动，均与功能实现无关、归属待确认~~ → ⭐ **已清理**：两处改动已还原为已提交状态，工作区干净。
9. **🟡 服务端助手域缺专项单测**：`AiAssistantChatServiceImpl` / `AiToolExecutor` / `AiConfirmTokenService` / `AiMinderTranslationTool` 暂无对应测试类（前端核心模块已覆盖），建议补单测以支撑回归防线。

---

## 七、核查证据索引

| 类别 | 路径 |
|---|---|
| 后端 AI 服务 | `server/src/main/java/io/github/xiaomisum/robotest/service/ai/`（52 类，含 `AiRegressionRecommendService(Impl)`、`AiPlanOrderRecommendServiceImpl`、`AiPlanOrderScoring`）；⭐助手域 `service/ai/assistant/`（工具框架 `AiTool`/`ToolRegistry`/`AiToolExecutor` + 四只读工具 + `WriteToolExecutor` + `AiConfirmTokenService` + `AiMinderTranslationTool` + `GetPlatformGuideTool`/`GuideKnowledgeBase`）、⭐`AiAssistantChatServiceImpl` |
| 后端 AI 实体 | `server/src/main/java/io/github/xiaomisum/robotest/model/entity/ai/`（9 类，⭐含助手实体 `AiConversation`、`AiMessage`） |
| 后端控制器 | `controller/project/AiCaseController`（含 `/plans/regression-recommend`、`/plans/{id}/order-recommend`、`/plans/{id}/order-reason`）、`AiReviewController`、`BugAiController`、`AiTaskController`；`controller/admin/AiConfigController`、`AiAgentController`；`controller/workspace/AiStatusController`、⭐`controller/workspace/AiAssistantChatController` |
| 数据库 DDL | `server/src/main/resources/db/v1.1.sql`（⭐含 `ai_conversation`/`ai_message` 建表与索引）、`v1.2.sql` |
| AI 资源 | `server/src/main/resources/ai/provider-presets.json`、`prompt-defaults.json`（⭐`assistant_chat`/`dsl_translation` 已启用）、⭐`server/src/main/resources/ai/guide/*.md`（8 个指引片段） |
| 前端 AI 页面 | `web/src/pages/admin/AiConfigPage.vue`、`AiAgentsPage.vue`；`web/src/pages/project/RequirementPoolPage.vue`、`BugCreatePage.vue`、`BugListPage.vue`（含聚类入口）、`PlanDetailPage.vue`（含回归子集推荐与执行顺序推荐入口） |
| 前端 AI 组件 | `web/src/components/project/minder/ai/`（生成/预览/挂载）、`ReviewAiCheckPanel.vue`、`ReviewAiSummary.vue`、`MissingPointsPanel.vue`、`BugAiSuggest.vue`、`BugDedupList.vue`、`BugClusterPanel.vue`、`RegressionRecommendDialog.vue`、`PlanOrderRecommend.vue`；⭐`web/src/components/assistant/`（`AssistantPanel.vue`、`AssistantFab.vue`、`MessageItem.vue`、`DslPreviewDialog.vue`、`assistantConfirm.ts`、`assistantLinkWhitelist.ts`、`dslRunner.ts`） |
| 前端 AI 基建 | `web/src/services/ai.ts`（含 `regressionRecommend`、`planOrderRecommend`、⭐助手会话/SSE 接口）、`web/src/stores/ai.ts`、⭐`web/src/stores/assistantContext.ts`、`web/src/composables/useAiStream.ts`、⭐`useAssistantStream.ts`、⭐`useConversationList.ts`、`useBugDedup.ts`、`components/common/AiModelSelect.vue`；⭐入口挂载 `web/src/layouts/BusinessLayout.vue` |
| 提交记录 | 分支 `feature/v1.1-ai-agents`；初版基线 `d4d2235`，⭐最新 `24cd518`（2026-08-04）；本次纳入 `2367d66`（⭐助手会话与只读工具）/ `8ed8f6b`（⭐Function Calling 与写工具确认）/ `927824f`（⭐脑图指令翻译）/ `c51ad87`（⭐助手前端链路）/ `fecffdb`（⭐面板滚动适配）/ `f80b31a`（⭐悬浮入口挂载）/ `24cd518`（⭐助手核心单测） |
