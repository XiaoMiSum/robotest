# AI 任务执行模板

**文档版本**：V1.0
**日期**：2026-09-24
**状态**：已发布

---

> 本模板适用于 AI 编程助手执行代码、配置和文档任务。纯咨询或只读评审可以裁剪不适用步骤，但必须说明裁剪原因。

## 1. 任务信息

- 任务标题：
- 任务类型：功能 / 修复 / 重构 / 文档 / 安全 / 数据库 / 其他
- 当前分支：
- 目标分支：
- 关联需求、缺陷或 issue：
- 是否涉及前端：
- 是否涉及后端：
- 是否涉及 API/OpenAPI：
- 是否涉及数据库/迁移：
- 是否涉及部署/配置：

## 2. 理解

必须阅读：

- [ ] `docs/00-spec/01-readme.md`
- [ ] `docs/00-spec/02-overview.md`
- [ ] 与任务相关的端级规范
- [ ] `docs/01-requirements/` 对应需求
- [ ] `docs/02-high-level-design/` 对应概要设计
- [ ] `docs/03-architecture/` 对应架构文档
- [ ] `docs/04-detailed-design/` 对应详细设计
- [ ] `docs/05-interaction-design/` 对应交互文档

填写：

- 需求摘要：
- 当前行为：
- 期望行为：
- 业务边界：
- 技术影响范围：
- 术语和假设：

## 3. 一致性评估

> 如果当前实现、目标规范或设计文档之间存在冲突，必须暂停并请求用户确认。

| 检查项 | 结论 | 证据/差异 |
| --- | --- | --- |
| 需求与设计 | 一致 / 不一致 |  |
| 设计与当前实现 | 一致 / 不一致 |  |
| 规范与当前实现 | 一致 / 不一致 |  |
| 是否改变公共契约 | 否 / 是 |  |

差异点：

| 差异 | 现有描述及出处 | 任务要求 | 影响 |
| --- | --- | --- | --- |
|  |  |  |  |

用户确认结果：

- [ ] 以现有文档为准
- [ ] 以任务为准
- [ ] 需要更新规范/设计后继续

确认后先更新对应文档，再进入探查和编码。

## 4. 探查

记录已查看的相邻代码和模式：

- Controller / Service / Mapper：
- 页面 / 组件 / composable / service / store：
- DTO / Entity / 类型：
- 配置文件 / 脚本 / 测试：
- 相关 AGENTS 和详细设计：

确认的命名、导入、类型、错误处理和测试模式：

- 命名规范：
- 依赖方向：
- API 契约：
- 数据访问：
- UI 状态：
- 安全边界：

## 5. 方案

### 5.1 文件变更

| 文件 | 新建/修改 | 变更说明 |
| --- | --- | --- |
|  |  |  |

### 5.2 API 变更

- 方法：
- URL：
- 请求头和鉴权：
- 请求：
- `Result` 响应：
- 分页：`pageNo/pageSize`、`list/total`：
- 10 位错误码：
- OpenAPI 和前端类型：
- 兼容性和弃用策略：

### 5.3 数据库变更

- 数据库：PostgreSQL 14+ / 其他兼容说明
- 迁移版本：
- DDL：
- 数据回填：
- 索引：
- 回滚：
- 锁表和发布窗口：

### 5.4 前端变更

- 页面和组件：
- composable/store/service：
- 加载、空态、错误态：
- 可访问性和响应式：
- 测试：

### 5.5 安全和部署

- 认证/授权影响：
- 作用域隔离：
- 密钥和日志：
- 构建、迁移和回滚：
- 新增依赖及必要性：

### 5.6 风险和回滚

- 风险：
- 回滚方案：
- 需要用户确认的开放问题：

> 方案输出后必须等待用户确认，确认前不得编码。

## 6. 编码

- [ ] 遵循 C1-C11 和端级 AGENTS
- [ ] 未引入无理由的 `any`、物理外键或绕过统一异常
- [ ] 上下文边界符合业务详细设计且服务端完成归属校验
- [ ] 更新采用部分更新策略
- [ ] 注释只解释 why
- [ ] 新增依赖已说明必要性

## 7. 验证

### 7.1 前端

```bash
cd web
pnpm run lint
pnpm run format:check
pnpm run typecheck
pnpm run test:unit
pnpm run build
```

### 7.2 后端

```bash
cd server
mvn test
mvn verify
```

### 7.3 统一检查

```bash
bash scripts/validate.sh --all
```

### 7.4 手工和契约验证

- [ ] OpenAPI 已重新生成或确认无变化
- [ ] 关键 API 手工验证
- [ ] 登录和 Token 流程验证
- [ ] 作用域越权验证
- [ ] WebSocket 实时消息关键流程验证
- [ ] 数据库迁移和回滚验证
- [ ] 部署后健康检查（如适用）

验证结果：

- 命令：
- 结果：
- 报告或证据：
- 未执行项目及原因：

## 8. 自检

| 规则 | 检查 | 结果 | 证据 |
| --- | --- | --- | --- |
| C1 | 无 `any`，类型安全 |  |  |
| C2 | Controller 无业务逻辑 |  |  |
| C3 | 使用统一业务异常和 10 位错误码 |  |  |
| C4 | 上下文边界符合业务详细设计且服务端完成归属校验 |  |  |
| C5 | 数据库字段和无物理外键 |  |  |
| C6 | 注释只写 why |  |  |
| C7 | 提交格式和原子性 |  |  |
| C8 | 覆盖率门禁 |  |  |
| C9 | 索引规范 |  |  |
| C10 | 优先复用 migoo |  |  |
| C11 | 部分更新 |  |  |
| UI-SC | 滚动专项（如适用） |  |  |

## 9. 提交和交付

- [ ] 验证完成后才生成提交
- [ ] 当前分支和目标分支正确
- [ ] 提交信息符合 C7
- [ ] PR 关联需求、风险、验证和回滚信息
- [ ] 未主动 merge 或 push

提交记录：

| commit | message | 说明 |
| --- | --- | --- |
|  |  |  |

交付说明：

- 改动内容：
- 兼容性影响：
- 已知限制：
- 后续任务：
- 文档同步情况：

---

**文档结束**
