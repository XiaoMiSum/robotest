# 任务执行模板

> AI 编程助手执行开发任务时，**必须**按此模板逐项填写，禁止跳步。
> 每一步完成后才能进入下一步。步骤 2（一致性评估）发现不符时必须等待用户确认并先更新文档；步骤 4（方案）必须等待用户确认后才能进入步骤 5（编码）。

---

## 1. 理解

**阅读的文档**：
- [ ] `docs/spec/` 相关规范
- [ ] `docs/需求/` 对应业务文档
- [ ] `docs/概要/` / `docs/详细设计/` 对应文档
- [ ] `docs/交互设计/` 对应页面文档

**需求摘要**：
（简要描述本次任务要做什么）

**涉及范围**：
- 前端：（列出涉及的目录/文件）
- 后端：（列出涉及的目录/文件）
- 数据库：（是/否，涉及哪些表）
- API：（是/否，新增/变更哪些接口）

---

## 2. 一致性评估（⚠️ 不符时暂停，等待用户确认）

**评估结论**：
- [ ] 与现有需求/设计文档一致，直接进入探查
- [ ] 与现有文档不符，差异点如下（等待用户确认）

**差异点**（如适用）：
| 差异点 | 现有文档描述（含出处） | 本次任务要求 |
|-------|---------------------|------------|
|       |                     |            |

**用户确认结果**（如适用）：
- 以文档为准 / 以任务为准

**文档更新记录**（确认以任务为准后，先更新文档再进入探查）：
| 文档路径 | 更新说明 |
|---------|---------|
|         |         |

---

## 3. 探查

**查看的现有代码**：
（列出查看了哪些相邻文件，确认了哪些模式）

**确认的模式**：
- 命名规范：
- 导入路径：
- 类型定义：
- 代码风格：

---

## 4. 方案（⚠️ 输出后暂停，等待用户确认）

**需新建的文件**：
| 文件路径 | 类型 | 说明 |
|---------|------|------|
|         |      |      |

**需修改的文件**：
| 文件路径 | 变更说明 |
|---------|---------|
|         |         |

**数据库变更**（如适用）：
```sql
-- DDL 变更
```

**API 变更**（如适用）：
- 方法：
- URL：
- 请求：
- 响应：

**新增依赖**（如适用）：
- 无 / （说明原因和必要性）

---

## 5. 编码

**提交记录**：
| commit hash | commit message | 变更说明 |
|-------------|---------------|---------|
|             |               |         |

---

## 6. 验证

**执行的命令**：
```bash
# 前端验证（如适用）
pnpm run lint
pnpm run typecheck
pnpm run test:unit

# 后端验证（如适用）
mvn test
```

**验证结果**：
- [ ] lint 通过
- [ ] typecheck 通过
- [ ] 测试通过
- [ ] 覆盖率达标

---

## 7. 自检

| 编号 | 规则 | 是否通过 |
|------|------|---------|
| C1   | 无 `any`（前端） | [ ] |
| C2   | 无业务逻辑在 Controller | [ ] |
| C3   | 异常使用 BusinessException | [ ] |
| C4   | 上下文头传递，不出现在 URL | [ ] |
| C5   | 数据库迁移说明，无物理外键 | [ ] |
| C6   | 注释只写 why | [ ] |
| C7   | 提交格式 `<emoji> <type>(<scope>): <description>` | [ ] |
| C8   | 覆盖率达标 | [ ] |

---

## 8. 交付

**改动说明**：
（向用户说明改了什么、为什么这么改）

**测试结果**：
（说明验证结果）

**注意**：
- 不主动合并或 push，待用户确认后发起 PR。
