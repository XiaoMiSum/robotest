# 软件测试平台——全局智能助手交互设计

**文档版本**：V1.1  
**日期**：2026-07-31  
**作者**：[填写]  
**状态**：起草中

> AI 通用交互规范见《AI基础设施与管理端页面交互设计》第 2 章，本文档不再重复。  
> 覆盖用户故事：US-AI-011（自然语言查询）、US-AI-012（快捷操作执行）、US-AI-013（对话式脑图编辑）、US-AI-014（平台使用指引）。

---

## 1. 总体布局

### 1.1 悬浮按钮与面板

助手悬浮按钮挂载于 BusinessLayout（业务模式：工作空间/项目页面），管理端布局不挂载；随 `aiEnabled` 显隐。`aiEnabled` 为**全局 AI 开关**（`GET /api/workspace/ai/status` 不依赖工作空间上下文）：未选择工作空间（含空间列表页 `/workspaces`）时按钮仍显示；点击提示「请先进入工作空间」且**不打开面板**（见 2 节）；面板打开后中途离开工作空间时显示「请先选择工作空间」引导态兜底。

> **零影响约束（通用规范 2.7）**：悬浮按钮固定于右下角浮层，不占用页面内容区、不遮挡既有操作；`aiEnabled = false` 时按钮与面板整体隐藏，业务页面回退 V1.0 形态。助手的写操作与脑图编辑均经预览-确认-执行并复用既有 Service/编辑通道（权限、校验、审计不变），不存在绕过既有规则影响原有功能的路径。

```
┌──────────────────────────────────────────────────────────┐
│  顶部导航栏                                              │
├──────────────────────────────────────┬───────────────────┤
│                                      │  助手对话面板     │
│           业务页面内容区              │ （右侧抽屉滑出）  │
│                                      │                   │
│                                      │                   │
│                                 (✨) │                   │
└──────────────────────────────────────┴───────────────────┘
        右下角悬浮按钮 ↑
```

### 1.2 对话面板结构

```
┌───────────────────────────────────────────┐
│  ✨ 智能助手                      ─  ✕    │
├──────────┬────────────────────────────────┤
│ 会话列表  │  消息流                        │
│ [＋新会话]│  ┌──────────────────────────┐ │
│ ·登录评审 │  │ 用户: 我最近一周的评审？  │ │
│ ·缺陷统计 │  │ 助手: (流式渲染中…▌)      │ │
│ ·…       │  │  🔧 工具过程卡片          │ │
│ (滚动加载)│  │  📋 写操作确认卡片        │ │
│          │  └──────────────────────────┘ │
│          │  [输入消息…          ] [发送]  │
│          │  [模型: DeepSeek-V3 ▼]         │
│          │            （流式期间：[停止]）│
└──────────┴────────────────────────────────┘
```

---

## 2. 悬浮按钮

| 操作 | 触发方式 | 反馈 |
| ---- | ---- | ---- |
| 打开面板 | 点击悬浮按钮 | 右侧抽屉滑出；恢复最近一次会话（若有） |
| 未选择工作空间时打开 | 在 `/workspaces` 等未选空间页面点击悬浮按钮 | Toast 提示「请先进入工作空间」，不打开面板；选择工作空间后点击即可正常打开 |
| 最小化 | 面板 [─] 按钮 | 面板收起为悬浮按钮，**会话状态保持**（含进行中的流式回复） |
| 关闭 | 面板 [✕] 按钮 | 面板关闭；再次打开时重载会话列表 |
| 空间切换 | 用户切换工作空间 | 面板重置会话列表（会话按空间隔离），当前会话关闭 |

---

## 3. 会话管理

| 操作 | 触发方式 | 反馈 |
| ---- | ---- | ---- |
| 新建会话 | 点击 [＋新会话] | 消息流清空，首条消息发送后会话入列并本地置顶 |
| 切换会话 | 点击会话列表项 | 惰性拉取该会话全量消息渲染；50 条以上启用虚拟滚动 |
| 滚动加载 | 会话列表滚动至底部 | 携带游标拉取下一页追加渲染（加载中显示骨架条）；无更多后不再触发 |
| 发送消息置顶 | 发送消息 / 新建会话 | 对应会话本地置顶，不重拉列表 |
| 清空会话 | 会话项操作菜单 [清空] | 二次确认 → 删除该会话记录 |

- 会话内容仅本人可见；会话按工作空间隔离。

---

## 4. 消息交互

### 4.1 消息发送与流式回复

| 操作 | 触发方式 | 反馈 |
| ---- | ---- | ---- |
| 发送消息 | 输入 + 回车 / [发送] | 用户消息即时上屏；助手回复 SSE 流式逐字渲染 |
| 切换对话模型 | 输入区工具条模型选择器（`AiModelSelect`，通用规范见《AI基础设施与管理端页面交互设计》2.8） | 即时生效于后续消息（历史消息不受影响）；选择本地记忆，失效静默回退系统默认；仅一个可用模型时不渲染 |
| 流式期间 | 回复进行中 | **输入框禁用** + [发送] 变为 [停止] 按钮 |
| 停止回复 | 点击 [停止] | 断开 SSE（服务端联动取消上游），已渲染内容保留为最终消息 |
| 超时提示 | 首帧超 10 秒 | 消息气泡内提示「响应较慢，可停止重试」（通用规范 2.3） |

### 4.2 消息渲染规则

- 助手消息以 Markdown 渲染，**链接白名单过滤**：仅渲染站内相对路径（`/` 开头且匹配已注册路由前缀），外部 URL 一律降级为纯文本；
- 查询结果以列表 / 数字 / 站内链接形式呈现，点击链接跳转对应业务页面；
- 查询不到的数据，助手明确回复「查询不到」，不虚构。

### 4.3 工具过程卡片

助手调用只读工具（查询评审 / 计划 / 缺陷 / 用例）时，消息流内插入过程卡片：

```
┌────────────────────────────┐
│ 🔧 正在查询：我参与的评审   │  ← 执行中：loading 态
│ ✓ 查询完成（5 条结果）      │  ← 完成：结果概要
└────────────────────────────┘
```

- 过程卡片默认折叠为一行，可展开查看调用摘要；用户无权限的数据不返回、不展示。
- 过程卡片视觉规格（底色/圆角/状态图标/徽标）见 9.4。

---

## 5. 快捷操作执行（US-AI-012）

写操作（白名单制，首期仅创建缺陷、创建计划草稿）强制预览-确认-执行，通过**确认卡片**承载：

### 5.1 确认卡片布局

```
┌──────────────────────────────────────┐
│ 📋 待确认操作：创建缺陷               │
│ ┌──────────────────────────────────┐ │
│ │ 项目     │ 项目X                  │ │
│ │ 标题     │ 登录超时               │ │
│ │ 优先级   │ P1（高）               │ │
│ │ …字段明细表格…                    │ │
│ └──────────────────────────────────┘ │
│ 剩余确认时间 04:32                    │
│              [取消]  [确认执行]       │
└──────────────────────────────────────┘
```

### 5.2 交互说明

| 操作 | 触发方式 | 反馈 |
| ---- | ---- | ---- |
| 收到确认请求 | 助手判定需执行写操作 | 消息流插入确认卡片（操作明细表格 + 倒计时），流式循环暂停等待 |
| 确认执行 | 点击 [确认执行] | 卡片状态更新为「执行成功」+ 结果跳转链接；助手生成收尾答复 |
| 取消 | 点击 [取消] | 卡片置「已取消」，操作放弃，助手答复确认取消 |
| 确认超时 | 倒计时归零（默认 5 分钟） | 卡片置「已超时」不可操作，操作放弃 |
| 执行失败 | 后端校验失败 | 卡片展示失败原因（与人工操作相同的业务校验与提示） |

- 写操作与人工操作产生完全相同的审计记录与业务校验；任何写操作不得跳过确认。

---

## 6. 对话式脑图编辑（US-AI-013）

### 6.1 页面上下文桥

- 脑图编辑页挂载时向助手注入上下文：当前项目、当前文档、选中节点；选中变化实时更新；
- 非脑图页仅注入当前项目（若在项目内）；
- 上下文使助手能消歧「当前用例」「这个节点」等指代。

### 6.2 交互说明

| 操作 | 触发方式 | 反馈 |
| ---- | ---- | ---- |
| 发出编辑指令 | 脑图页内对助手说「为当前用例增加一个步骤…」 | 助手翻译为脑图操作指令，回复「已准备好编辑预览，请确认」 |
| 执行预览 | 消息流内 DSL 预览执行入口 | 弹出**命中预览确认弹窗**（命中数量 + 清单 + 跳过项） |
| 确认执行 | 预览弹窗确认 | 前端经编辑内核批量执行，单条撤销历史；新增节点带 AI 徽标；消息流追加本地提示「已执行」 |
| 取消执行 | 预览弹窗取消 | 消息流追加本地提示「已取消」，无任何变更 |
| 已离开文档页 | 编辑指令到达时不在对应文档 | 提示「请回到文档后重试」，指令丢弃不缓存 |
| 无文档上下文 | 非脑图页发出编辑指令 | 助手回复「请在脑图编辑页使用该能力」 |
| 指令歧义 | 翻译置信不足 | 助手以文本追问澄清，不做模糊执行 |
| 无编辑权限 | 权限校验失败 | 助手回复无权限提示 |

---

## 7. 平台使用指引（US-AI-014）

| 操作 | 触发方式 | 反馈 |
| ---- | ---- | ---- |
| 询问使用方法 | 如「如何发起评审？」 | 助手返回步骤指引 + 可点击的页面路由链接 |
| 点击指引链接 | 消息内站内链接 | 跳转对应功能页面（面板保持最小化可回） |
| 知识库外问题 | 无命中知识片段 | 明确回复「该问题超出使用指引范围」，不杜撰功能 |

- 使用指引为纯只读能力，任何角色的业务用户均可使用；指引内容按用户角色过滤（不展示无权使用的功能指引）。

---

## 8. 通用交互模式总结

### 8.1 卡片类型

| 类型 | 使用场景 |
| ---- | ---- |
| 工具过程卡片 | 只读工具调用过程与结果概要 |
| 确认卡片 | 写操作预览-确认-执行（含倒计时） |
| DSL 预览入口 | 对话式脑图编辑的执行确认 |

### 8.2 加载状态

- 消息回复：流式逐字渲染 + 打字光标；输入框禁用 + [停止] 按钮；
- 会话列表：滚动加载骨架条；
- 消息历史：50 条以上虚拟滚动，保障长会话性能。

### 8.3 状态保持与重置

| 场景 | 行为 |
| ---- | ---- |
| 最小化 | 会话与流式回复保持 |
| 页面路由切换（同空间） | 面板状态保持 |
| 工作空间切换 | 会话列表重置，当前会话关闭 |
| 未选择工作空间 | 悬浮按钮随全局 `aiEnabled` 保持显示；点击提示「请先进入工作空间」不打开面板；面板已打开时（中途离开空间）显示「请先选择工作空间」引导态，无会话能力 |
| AI 开关关闭 | 悬浮按钮与面板隐藏，进行中回复中断并提示 |

---

## 9. 视觉与状态规范

> 本章为智能助手 UI 视觉基线，对应实现：`web/src/components/assistant/`（AssistantPanel / MessageItem / AssistantFab）。
> 所有颜色 / 圆角 / 阴影 / 过渡令牌均来自《视觉设计.md》第 7 节 CSS 变量定义（实现于 `web/src/assets/styles/variables.scss`），本章不另行定义色值；图标一律使用 SVG 组件（Sparkles / Check / Close / Wrench / Clipboard），禁止 emoji（正文 ASCII 示意图中的 emoji 仅为标记）。

### 9.1 设计方向

| 现状问题 | 目标（参考主流 AI 助手） |
| --- | --- |
| 无头像，缺乏"助手感" | AI 品牌渐变头像 + 用户头像，消息左右对齐分明 |
| 用户气泡浅蓝、assistant 裸文本 | 用户主色实心气泡白字；assistant 顶格内容 + 头像列 |
| emoji 图标（✨） | 统一 SVG 图标 |
| 面板头部一行字、无氛围 | 头部信息完整（标题 + 状态），底部细分隔线 |
| 输入区普通文本框 | 胶囊圆角 + 聚焦光环 + 渐变发送按钮 |
| 无进入动效 | GPU 合成过渡（仅 transform/opacity），克制不喧宾夺主 |

### 9.2 视觉令牌（引用《视觉设计.md》§7）

- 主色：`--color-primary-400/500/600/700`、`--color-primary-50/100`
- 中性：`--color-neutral-0/50/100/200/300/400/500/600/700/800`
- 语义：`--color-success`、`--color-danger`、`--color-info`、`--color-info-light`
- 圆角：`--radius-sm(4)` / `md(6)` / `lg(8)` / `xl(12)`
- 阴影：`--shadow-sm` / `md` / `lg` / `card` / `card-hover`
- 过渡：`--transition-fast(0.15s)` / `base(0.2s)` / `slow(0.3s)`
- 间距：`--space-xs(4)` / `sm(8)` / `md(12)` / `lg(16)` / `xl(24)`
- 主色透明变体（仅限两处光晕场景，为主色 500 的 alpha 派生，不新增色板）：输入聚焦光环 `rgba(59, 130, 246, 0.12)`；悬浮球外圈光晕 `rgba(59, 130, 246, 0.15)`

### 9.3 消息视觉

#### 9.3.1 AI 头像（assistant 消息）
- 尺寸：28px 圆角方形，`border-radius: var(--radius-lg)`
- 底：`linear-gradient(135deg, var(--color-primary-400), var(--color-primary-700))`
- 内容：白色 SVG Sparkles（14px，非 emoji）
- 位置：消息左侧，与内容列 `gap: var(--space-md)`；头像与首行文字顶端对齐

#### 9.3.2 用户头像
- 尺寸：28px 圆角方形
- 内容：优先显示当前登录用户信息的头像 URL（`avatarUrl`，图片 `object-fit: cover` 铺满）；无头像时回退用户名首字符（`--color-neutral-600` 粗体）
- 底：`var(--color-neutral-200)`（仅回退首字符时可见）
- 位置：用户气泡右侧
- 数据来源：消息模型不携带用户信息，取 `authStore` 当前登录用户

#### 9.3.3 用户消息气泡
- 底：`var(--color-primary-500)`，文字 `--color-neutral-0`
- 圆角：`var(--radius-xl)`，右上角收为 `var(--radius-md)`（小角指向头像）
- 最大宽度：容器 82%；内边距 `10px 14px`
- 字体：13px，行高 1.6

#### 9.3.4 assistant 内容列
- 无气泡底色（顶格排版，ChatGPT 模式），宽度 100%
- Markdown 正文：13.5px，行高 1.75，`color: var(--color-neutral-700)`
- 代码块 / 引用 / 列表沿用现有 DSL 样式，仅间距与圆角对齐令牌

#### 9.3.5 消息组间距
- 组内（消息 + 过程卡片）：`gap: var(--space-sm)`
- 组间：`margin-top: var(--space-xl)`
- 消息区水平内边距：`--space-lg`

### 9.4 工具过程卡片（交互行为见 4.3）

- 底：`var(--color-neutral-50)`，圆角 `var(--radius-lg)`，边框 `1px solid var(--color-neutral-100)`
- 状态图标：运行中为加载旋转动画（主色）；完成为 SVG ✓（`--color-success`）；失败为 SVG ✕（`--color-danger`）
- 状态徽标：运行中 `--color-info-light` 底 + `--color-info` 字；完成 `--color-success` 字
- 展开 / 收起箭头 hover 过渡

### 9.5 流式光标

- 保留 `▍` 字符光标，`color: var(--color-primary-500)`，`animation: blink 1s step-end infinite`

### 9.6 面板头部

- 高度 52px，`padding: 0 var(--space-lg)`，`border-bottom: 1px solid var(--color-neutral-100)`
- 标题行：SVG Sparkles（主色渐变底圆角小方块）+「智能助手」（600 字重，14px）
- 副信息（模型名 / 状态点）：12px，`--color-neutral-400`；在线状态点 `--color-success`
- 右侧按钮：icon-only，hover `--color-neutral-100` 圆角底

### 9.7 会话列表

- 激活项：`--color-primary-50` 底 + 左 3px `--color-primary-500` 圆角条，过渡 `--transition-fast`
- hover：`--color-neutral-100`
- 标题 13px / 时间 11px，`--color-neutral-400`
- [＋新会话] 按钮右对齐（列表顶部右缘，与消息区对齐）

### 9.8 输入区

- textarea 容器：圆角 `var(--radius-xl)`，边框 `1px solid var(--color-neutral-200)`
- 自动增高：`autosize`（minRows 1 / maxRows 3），输入超出可视宽度自动换行并增高，达到 3 行上限后内部滚动（`overflow-y: auto`）
- 聚焦：`box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.12)` + 边框 `--color-primary-500`
- 发送按钮：`--color-primary-500` 底白字，圆角 `var(--radius-md)`；hover `--color-primary-600`；禁用态 `--color-neutral-200`
- 工具栏操作组（提示行 + 发送/停止）恒靠右（`margin-left: auto`）：单一可用模型时模型选择器不渲染（见 4.1），避免操作组落入左侧
- 占位符：`--color-neutral-400`；提示行 11px `--color-neutral-400`

### 9.9 悬浮球（AssistantFab）

- 尺寸 48px 圆形，渐变底不变（primary-400 → 600）
- 内容：SVG Sparkles 白图标（20px，非 emoji）
- 光晕：外圈 `box-shadow: 0 0 0 4px rgba(59, 130, 246, 0.15)` + `--shadow-lg`；hover 放大 + 光晕增强
- 拖拽逻辑、显隐逻辑不变

### 9.10 动效规范

- 一律用 `transform` / `opacity`（GPU 合成），禁止重排属性动画
- 消息进入：`opacity 0 → 1` + `translateY(8px) → 0`，`--transition-slow`，`animation-fill-mode: both`
- hover 过渡：`--transition-fast`
- 光标 blink：`1s step-end infinite`（两端不可见）
- 面板开合过渡（若存在）保持既有行为

### 9.11 设计约束

- ❌ 新增 CSS 变量 / 色值 / 第三方库
- ❌ 修改组件 `<script>` 逻辑（事件、状态、请求、滚动）
- ❌ 全局样式污染（所有样式保持 scoped，必要时 `:deep()` 限定在组件子树内）
- ❌ emoji 作图标（一律 SVG；示意图 emoji 仅为标记）
- ❌ 大幅增加文件行数（优先替换既有样式，保持 diff 精简）

---

**文档结束**
