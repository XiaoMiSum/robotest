# 软件测试平台——AI基础设施与管理端页面交互设计

**文档版本**：V1.1  
**日期**：2026-07-31  
**作者**：[填写]  
**状态**：起草中

> 本文档为 V1.1 AI 能力域交互设计的公共基线：第 2 章「AI 通用交互规范」被其余四份 V1.1 交互设计文档引用，阅读顺序建议先读本文档。  
> 导航框架与色彩体系分别见《全局导航与菜单交互系统设计》《视觉设计》，本文档只描述 V1.1 增量。

---

## 1. 概述

V1.1 新增 AI 智能辅助能力域。本文档覆盖两部分交互设计：

1. **AI 通用交互规范**——所有 AI 功能入口共享的加载、取消、超时、降级、开关显隐、预览-确认-执行、对既有功能零影响等横切交互约定（承接 SRS 4.1/4.3/4.4）。
2. **管理端新增页面**——「AI 配置」页与「智能体」页（AdminLayout 左侧菜单新增两项，仅系统管理员可见）。

---

## 2. AI 通用交互规范

### 2.1 AI 入口统一视觉标识

- 所有 AI 功能入口（按钮、菜单项、输入区）统一附加 AI 标识：Element Plus `MagicStick` 图标 + 渐变强调色（主色→紫色 `#A464FF` 渐变），用户可一眼识别该操作由 AI 驱动。
- AI 生成的脑图节点持续显示「AI」徽标（复用既有节点徽标体系），落库、进入评审/计划快照后徽标持续可见。
- AI 产出的建议类内容（推荐标签、建议卡片）均带「AI」角标，与人工内容明确区分。

### 2.2 AI 开关显隐

- 前端启动后由 `stores/ai.ts` 缓存 `GET /api/workspace/ai/status` 结果，暴露 `aiEnabled` / `semanticSearch` / `chatModels`（可切换对话模型清单，供 2.8 选择器使用）三个状态。
- `aiEnabled = false` 时：**隐藏全部 AI 功能入口**（工具栏按钮、右键菜单项、表单按钮、悬浮按钮、面板入口等），不做置灰保留。
- **例外**：需求池入口为常规业务功能，不随 AI 开关隐藏；AI 不可用时条目管理照常可用。
- AI 开关由启用转关闭时：进行中的流式生成立即中断并提示用户；进行中的异步任务自动终止并标记「已取消」；此前已产出的结果仍可查看。

### 2.3 三类调用形态的加载与反馈

| 形态 | 适用功能 | 加载反馈 | 取消方式 |
| ---- | ---- | ---- | ---- |
| 同步调用 | 表单建议、优先级推荐、DSL 翻译 | 触发按钮 loading + 禁用，不阻塞页面其他操作 | 不提供取消（超时阈值短，10s 级自动失败） |
| 同步长调用 | 遗漏测试点分析、回归子集推荐 | 面板持续加载态（前端超时 70s） | 全程显示 [取消] 按钮，点击即中断请求（abort） |
| 流式调用（SSE） | 用例生成、文本导入解析、评审摘要、助手对话 | 输出区逐字渲染增量内容 + 打字光标动效 | 全程显示 [停止] 按钮，点击即断开 SSE |
| 异步任务 | 评审检查、缺陷聚类、向量重建 | 面板内进度条 + 状态文案，前端每 2 秒轮询任务状态 | 面板提供 [取消任务] 按钮 |

流式调用的超时提示（SRS 4.1）：

1. 首内容 3 秒内返回为目标值，此期间显示「AI 思考中…」加载态；
2. 超 10 秒未见首帧：加载态旁追加提示「响应较慢，可取消或重试」，并高亮 [停止] 按钮；
3. 单次生成总时长超 60 秒：允许用户随时取消，取消后已输出内容不保留。

异步任务离开页面后不中断，返回页面时面板依据任务状态接口恢复展示（任务状态即真相源）。

### 2.4 预览-确认-执行模式

所有涉及数据写入的 AI 操作强制遵循三段式：

```
AI 产出结果 → 预览展示（可勾选/可编辑/可取消） → 用户显式确认 → 前端/后端执行
```

- 脑图内操作（生成挂载、DSL 批量执行、对话式编辑）：执行后整批作为**单条撤销历史**，可一键撤销；
- 业务对象操作（助手创建缺陷/计划草稿）：执行前展示操作明细确认卡片，执行后附结果跳转链接可追溯；
- 任何写操作不得跳过确认环节。

### 2.5 非侵入式建议

建议类结果（优先级推荐、等级建议、疑似重复缺陷、遗漏测试点）一律以非阻断形式呈现：

- 不弹模态窗、不抢占焦点、不阻塞用户输入与提交；
- 用户可采纳（点击回填）、忽略（不操作）或手工另选；
- 用户已手工完成选择后到达的 AI 结果直接丢弃，不覆盖用户选择。

### 2.6 降级与失败提示

- 语义能力降级（`semanticSearch = degraded/unavailable`）时，相关功能自动切换关键词模式，界面顶部显示浅黄提示条：「语义检索暂不可用，当前为关键词匹配结果」；
- AI 调用失败：建议类功能仅轻提示（Toast），不影响表单/页面操作；生成类功能在输出区展示失败原因 + [重试] 按钮；
- 限流超限（错误码 6004）：Toast 明确提示「AI 调用过于频繁，请稍后再试」；
- 异步任务失败：面板展示明确失败原因，提供 [重试] 按钮。

### 2.7 对既有功能的零影响原则

新增 AI 功能区一律以**增量附加**方式嵌入，不得影响 V1.0 既有功能（承接 SRS 4.4）：

- **布局零侵入**：AI 入口（按钮、菜单项、面板、标签页、悬浮按钮）只在既有布局的空余位置追加，不改变既有控件的位置、尺寸、操作流程与快捷键；AI 附加面板/卡片区在无内容时不占位；
- **关闭即还原**：`aiEnabled = false` 或 AI 服务不可用时，全部 AI 功能区隐藏，页面布局与交互回退到与 V1.0 完全一致的形态（需求池除外，见 2.2）；
- **失败不阻断**：AI 调用失败、超时或降级时，既有功能（表单提交、脑图编辑、评审、计划、缺陷流转等）必须完全不受影响，AI 入口自动降级为不可用提示；
- **数据不越权**：AI 仅通过预览-确认-执行写入数据（2.4），不存在绕过用户确认自动修改既有业务数据的路径。

### 2.8 对话模型选择器

交互式 AI 功能（全局助手输入区、用例生成/步骤补全/文本导入弹窗、评审摘要生成等用户直接发起的功能）在入口附近提供统一的**对话模型选择器**（公共组件 `AiModelSelect`）：

- 数据源为能力开关接口下发的已启用模型清单（仅显示名与默认标记），默认模型带「默认」徽标；
- 默认选中用户上次选择（浏览器本地记忆，全局一份）；记忆的模型已被停用/删除时静默回退系统默认并清除记忆，不弹提示；
- 切换即生效并写入记忆，作用于本次与后续所有交互式调用；
- 仅一个可用模型时选择器不渲染（无选择意义）；
- 后台异步任务（评审完整性检查、缺陷聚类）与建议类功能（优先级推荐、缺陷表单建议等）固定使用系统默认模型，**不展示**选择器；
- 选择器为纯附加控件，遵循 2.7 零影响原则，不改变各功能既有操作流程。

---

## 3. 管理端菜单增量

AdminLayout 左侧菜单在既有菜单项之后新增两项（沿用既有菜单权限控制，仅系统管理员可见）：

```
┌──────────┐
│ 仪表盘   │
│ 用户管理 │
│ 工作空间 │
│ 角色管理 │
│ AI 配置  │  ← 新增
│ 智能体   │  ← 新增
└──────────┘
```

---

## 4. AI 配置页

**路由**：`/admin/ai-config`

### 4.1 页面布局

```
┌──────────────────────────────────────────────────────┐
│  AI 配置                          AI 总开关 [ ● 启用] │
├──────────────────────────────────────────────────────┤
│  [模型配置] [调用统计]                    ← 标签页    │
├──────────────────────────────────────────────────────┤
│  ┌─ 对话模型 ──────────────────────── [+ 新建模型] ┐  │
│  │  名称        │供应商 │模型名      │状态│默认│操作│  │
│  │  DeepSeek-V3 │深度求索│deepseek-chat│启用│ ★ │…  │  │
│  │  GPT-4o      │OpenAI │gpt-4o      │启用│   │…  │  │
│  │  （行内操作：编辑 / 测试 / 设为默认 / 停用 / 删除）│  │
│  └────────────────────────────────────────────────┘  │
│  ┌─ Embedding 模型 [已配置/未配置]（点击标题栏展开）┐  │
│  │  供应商        模型名     （标签置顶、两列等宽）│  │
│  │ [OpenAI ▾]    [text-… ▾]                     │  │
│  │  服务地址      向量维度                        │  │
│  │ [https://…]   [1536    ]                     │  │
│  │  API 密钥（整行）   独有配置项（两列）          │  │
│  │  ┌ 高级自定义参数（浅底折叠区）─────────────────┐ │  │
│  │  ⚠ 向量重建：进行中 ▓▓▓░░ / 失败 [重试]         │  │
│  │                  [连通性测试] [保存]            │  │
│  └────────────────────────────────────────────────┘  │
│  ┌─ 系统配置项（分组卡片表单，非 JSON 编辑）───────┐  │
│  │  ▾ 限流阈值                                    │  │
│  │    生成类调用上限 [20]   建议类调用上限 [60]↺  │  │
│  │  ▾ 语义查重 / 聚类分析 / 检索与推荐 …          │  │
│  │  ▾ 全局助手 / 长度限制 / 数据保留              │  │
│  │    （分组内两列等宽布局，标签置顶，权重组合占整行，│  │
│  │     修改即自动保存）                            │  │
│  └────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────┘

对话模型新建/编辑弹窗（宽 560px）：
┌──────────────────────────────┐
│  新建对话模型                 │
│  显示名    [DeepSeek-V3    ]  │
│  供应商    [深度求索 ▼]       │
│  服务地址  [https://...    ]  │
│  模型名    [deepseek-chat  ]  │
│  密钥      [已配置（末位…） ]  │
│  ── 独有配置项（随供应商）──  │
│  ▸ 高级自定义参数（折叠）     │
│  [连通性测试]   [取消] [保存] │
└──────────────────────────────┘
```

### 4.2 交互说明

| 操作 | 触发方式 | 反馈 |
| ---- | ---- | ---- |
| 进入页面 | 菜单点击 | 拉取供应商预设清单、对话模型列表与当前配置，表单回显（密钥脱敏） |
| 新建对话模型 | 点击 [+ 新建模型] | 打开弹窗；首个创建的模型保存后自动标记为默认 |
| 编辑对话模型 | 行内 [编辑] | 弹窗回显该模型配置（密钥脱敏，留空不修改） |
| 选择供应商（弹窗内） | 下拉选择 | 自动填充默认服务地址，按预设模板渲染独有配置项控件（取默认值） |
| 切换供应商（已有配置） | 下拉改选 | 二次确认弹窗：提示将重置地址与独有配置项，模型名与密钥保留待自行核对 |
| 选择通用兼容模式 | 下拉选 `custom` | 不渲染独有配置项区，仅保留高级自定义参数折叠区（自由键值编辑） |
| 填写密钥 | 输入框 | 占位符显示「已配置（末位 ****）」；留空提交表示不修改 |
| 连通性测试 | 弹窗内或行内 [测试] | 按钮 loading → 真实调用验证；弹窗内以当前填写值测试，行内以已保存配置测试；成功/失败 Toast + 明细 |
| 设为默认 | 行内 [设为默认] | 二次确认 → 星标即时转移；停用状态的模型该入口置灰 |
| 停用模型 | 行内 [停用] | 二次确认：提示已选择该模型的用户将自动回退系统默认；默认模型该入口置灰（提示先转移默认） |
| 删除模型 | 行内 [删除] | 二次确认（红色警示）；默认模型不可删除，入口置灰 |
| Embedding 组保存 | 点击卡片内 [保存] | 前端全量校验 → 提交；若 Embedding 模型或维度变更，提示将触发向量索引全量重建（重建期间语义能力降级，见 2.6） |
| 展开/收起 Embedding 组 | 点击卡片标题栏 | 默认收起，手动展开后展示配置表单与底部 [连通性测试] [保存]；表单标签置顶、两列等宽布局；标题栏状态标签按核心项是否填写实时显示「已配置 / 未配置」；无独立启停开关，该组是否启用由表单是否填写决定（核心项留空保存即视为未配置） |
| 编辑系统配置项 | 分组卡片表单直接修改 | 每个分组为浅色卡片，组内两列等宽布局（标签置顶，权重组合占整行，组内奇数项时最后一项补整行）；类型化控件（数字输入带取值范围与步进、多选框、权重组合），每项附说明文案与默认值提示；越界即时红字校验；值偏离默认时显示「已修改」标记与单项 [恢复默认↺]；修改防抖 800ms 自动保存（校验失败不保存并在底部提示「存在校验错误的修改未保存」），成功静默生效，底部状态区显示「已自动保存」 |
| 查看重建任务状态 | Embedding 组下方状态区（存在重建任务时展示） | 进行中：进度条 + 2 秒轮询；失败/已取消：红字原因 + [重试] 按钮（重试成功前语义能力维持降级） |
| 切换 AI 总开关 | 页头开关 | 关闭时二次确认：提示将中断进行中的 AI 调用并隐藏全部 AI 入口，确认后自动保存；开启时若无已启用对话模型则阻止并提示先新建；切换即自动保存 |
| 查看调用统计 | 切换 [调用统计] 标签页 | 按功能类型 / 工作空间 / 日期 / 模型四维度聚合的统计表格（调用次数、token 总用量、平均耗时、失败数），支持日期范围筛选与维度切换 |

- AI 总开关与系统配置项修改即自动保存（800ms 防抖），Embedding 组与对话模型保持手动 [保存]；
- 对话模型列表与 Embedding 组相互独立，可分别保存与测试；
- 独有配置项与高级自定义参数在提交时合并透传，重名以独有配置项控件值为准；
- 表单校验失败时定位到首个错误项并红字提示。

---

## 5. 智能体页

**路由**：`/admin/ai-agents`

### 5.1 页面布局

```
┌──────────────────────────────────────────────────────┐
│  ✦ 智能体                   14 全部 · 0 已自定义      │
│  配置各 AI 功能的提示词模板，默认内置，可自定义角色指令并│
│  一键恢复                                            │
├──────────────────────────────────────────────────────┤
│  ┌────────────┐ ┌────────────┐ ┌────────────┐        │
│  │ ◇[默认]    │ │ ◇[已自定义]│ │ ◇[默认]    │        │
│  │ 用例子树生成 │ │ 评审摘要生成 │ │ 全局助手对话 │        │
│  │ 根据需求条目…│ │ 汇总评审快照…│ │ 面向平台业务…│        │
│  │ ────────── │ │ ────────── │ │ ────────── │        │
│  │ 内置默认模板 │ │ admin·07-30 │ │ 内置默认模板 │        │
│  └────────────┘ └────────────┘ └────────────┘        │
│  （卡片悬停时右上角浮现 [编辑]，已自定义另含 [恢复默认]）│
└──────────────────────────────────────────────────────┘

编辑抽屉（右侧滑出，宽 600px）：
┌────────────────────────────────┐
│  ✦ 用例子树生成       [已自定义] │
│  📝 角色指令段                   │
│  [多行文本域，可直接编辑]        │
│  ╭─ 格式约束段编辑（高级）──────╮ │
│  │ 开启后可修改输出格式约束…  [○]│ │
│  ╰────────────────────────────╯ │
│  📄 输出格式约束段                │
│  [多行文本域，默认只读锁定]      │
│                     [取消] [保存]│
└────────────────────────────────┘
```

### 5.2 交互说明

| 操作 | 触发方式 | 反馈 |
| ---- | ---- | ---- |
| 查看模板 | 悬停卡片右上角 [编辑]，或直接点击卡片 | 抽屉展示当前生效模板（自定义或内置默认），来源以标签标注 |
| 编辑角色指令段 | 文本域直接编辑 | 实时字数统计，保存时校验非空 |
| 编辑格式约束段 | 打开高级开关 | 二次确认弹窗：说明修改可能导致结构化校验失败的风险，确认后文本域解锁 |
| 保存自定义 | 点击 [保存] | 落库覆盖记录，列表卡片状态变更为「已自定义」，Toast 成功 |
| 恢复默认 | 悬停卡片右上角 [恢复默认] | 二次确认 → 删除自定义记录，回退内置模板，抽屉内容刷新 |
| 关闭抽屉（有未保存修改） | 点击 [取消] 或遮罩 | 离开确认弹窗：「有未保存的修改，确定离开？」 |

- 卡片标题栏状态标签与图标底色按「默认 / 已自定义」区分；编辑操作默认隐藏，卡片悬停时浮现，避免视觉干扰；
- 自定义、恢复默认及高级开关变更均记录审计日志（界面无需额外操作，静默记录）；
- 抽屉内不提供删除智能体操作（模板位与功能枚举一一对应，只可覆盖或恢复）。

---

## 6. 通用交互模式总结

### 6.1 弹窗类型

| 类型 | 使用场景 |
| ---- | ---- |
| 确认弹窗 | 切换供应商、关闭 AI 总开关、开启格式约束段编辑、恢复默认模板、对话模型设为默认/停用/删除 |
| 表单弹窗 | 对话模型新建/编辑（见 4.1） |
| 抽屉 | 智能体模板编辑 |
| 提示条 | 语义降级明示、向量重建进行中 |

### 6.2 加载状态

- **表单提交 / 连通性测试**：按钮 loading + 禁用，完成恢复。
- **统计表格**：首次加载骨架屏，筛选刷新顶部进度条。
- **流式 / 异步反馈**：统一遵循 2.3 节三类调用形态约定。

### 6.3 响应式处理

- 配置页两组表单在小屏幕下由并列改为上下堆叠；
- 智能体编辑抽屉小屏幕下全屏展示。

---

**文档结束**
