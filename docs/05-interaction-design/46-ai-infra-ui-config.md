# 软件测试平台——（分册：AI 配置页）

**文档版本**：V1.0  
**日期**：2026-09-23  
**状态**：起草中

> 本分册由《》按功能模块拆分而来。前言、引言、数据设计与公共约定见总览分册 `45-ai-infra-ui-overview.md`；原章节编号保持不变，分册-章节对照见总览分册。

---

## 4. AI 配置页

**路由**：`/admin/ai-config`

### 4.1 页面布局

```
┌──────────────────────────────────────────────────────┐
│  AI 配置                          AI 总开关 [ ● 启用] │
├──────────────────────────────────────────────────────┤
│  [AI 配置] [智能体] [调用统计]              ← 标签页    │
├──────────────────────────────────────────────────────┤
│  ┌─ 对话模型 ──────────────────────── [+ 新建模型] ┐  │
│  │  名称        │供应商 │模型名      │状态│默认│操作│  │
│  │  DeepSeek-V3 │深度求索│deepseek-chat│启用│ ★ │…  │  │
│  │  GPT-4o      │OpenAI │gpt-4o      │启用│   │…  │  │
│  │  （行内操作：编辑 / 测试 / 设为默认 / 停用 / 删除）│  │
│  └────────────────────────────────────────────────┘  │
│  ┌─ Embedding 模型 [已配置/未配置]（点击标题栏展开）┐  │
│  │  供应商        模型名     （标签置顶、两列等宽）│  │
│  │ [OpenAI ▾]    [text-… ▾]                     │  │
│  │  服务地址      向量维度                        │  │
│  │ [https://…]   [1536    ]                     │  │
│  │  API 密钥（整行）   独有配置项（两列）          │  │
│  │  ┌ 高级自定义参数（浅底折叠区）─────────────────┐ │  │
│  │  ⚠ 向量重建：进行中 ▓▓▓░░ / 失败 [重试]         │  │
│  │                  [连通性测试] [保存]            │  │
│  └────────────────────────────────────────────────┘  │
│  ┌─ 系统配置项（分组卡片表单，非 JSON 编辑）───────┐  │
│  │  ▾ 限流阈值                                    │  │
│  │    生成类调用上限 [20]   建议类调用上限 [60]↺  │  │
│  │  ▾ 语义查重 / 聚类分析 / 检索与推荐 …          │  │
│  │  ▾ 全局助手 / 长度限制 / 数据保留              │  │
│  │    （分组内两列等宽布局，标签置顶，权重组合占整行，│  │
│  │     修改即自动保存）                            │  │
│  └────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────┘

对话模型新建/编辑弹窗（宽 560px）：
┌──────────────────────────────┐
│  新建对话模型                 │
│  显示名    [DeepSeek-V3    ]  │
│  供应商    [深度求索 ▼]       │
│  服务地址  [https://...    ]  │
│  模型名    [deepseek-chat  ]  │
│  密钥      [已配置（末位…） ]  │
│  ── 独有配置项（随供应商）──  │
│  ▸ 高级自定义参数（折叠）     │
│  [连通性测试]   [取消] [保存] │
└──────────────────────────────┘
```

### 4.2 交互说明

| 操作 | 触发方式 | 反馈 |
| ---- | ---- | ---- |
| 进入页面 | 菜单点击 | 拉取供应商预设清单、对话模型列表与当前配置，表单回显（密钥脱敏） |
| 新建对话模型 | 点击 [+ 新建模型] | 打开弹窗；首个创建的模型保存后自动标记为默认 |
| 编辑对话模型 | 行内 [编辑] | 弹窗回显该模型配置（密钥脱敏，留空不修改） |
| 选择供应商（弹窗内） | 下拉选择 | 自动填充默认服务地址，按预设模板渲染独有配置项控件（取默认值） |
| 切换供应商（已有配置） | 下拉改选 | 二次确认弹窗：提示将重置地址与独有配置项，模型名与密钥保留待自行核对 |
| 选择通用兼容模式 | 下拉选 `custom` | 不渲染独有配置项区，仅保留高级自定义参数折叠区（自由键值编辑） |
| 填写密钥 | 输入框 | 占位符显示「已配置（末位 ****）」；留空提交表示不修改 |
| 连通性测试 | 弹窗内或行内 [测试] | 按钮 loading → 真实调用验证；弹窗内以当前填写值测试，行内以已保存配置测试；成功/失败 Toast + 明细 |
| 设为默认 | 行内 [设为默认] | 二次确认 → 星标即时转移；停用状态的模型该入口置灰 |
| 停用模型 | 行内 [停用] | 二次确认：提示已选择该模型的用户将自动回退系统默认；默认模型该入口置灰（提示先转移默认） |
| 删除模型 | 行内 [删除] | 二次确认（红色警示）；默认模型不可删除，入口置灰 |
| Embedding 组保存 | 点击卡片内 [保存] | 前端全量校验 → 提交；若 Embedding 模型或维度变更，提示将触发向量索引全量重建（重建期间语义能力降级，见 2.6） |
| 展开/收起 Embedding 组 | 点击卡片标题栏 | 默认收起，手动展开后展示配置表单与底部 [连通性测试] [保存]；表单标签置顶、两列等宽布局；标题栏状态标签按核心项是否填写实时显示「已配置 / 未配置」；无独立启停开关，该组是否启用由表单是否填写决定（核心项留空保存即视为未配置） |
| 编辑系统配置项 | 分组卡片表单直接修改 | 每个分组为浅色卡片，组内两列等宽布局（标签置顶，权重组合占整行，组内奇数项时最后一项补整行）；类型化控件（数字输入带取值范围与步进、多选框、权重组合），每项附说明文案与默认值提示；越界即时红字校验；值偏离默认时显示「已修改」标记与单项 [恢复默认↺]；修改防抖 800ms 自动保存（校验失败不保存并在底部提示「存在校验错误的修改未保存」），成功静默生效，底部状态区显示「已自动保存」 |
| 查看重建任务状态 | Embedding 组下方状态区（存在重建任务时展示） | 进行中：进度条 + 2 秒轮询；失败/已取消：红字原因 + [重试] 按钮（重试成功前语义能力维持降级） |
| 切换 AI 总开关 | 页头开关 | 关闭时二次确认：提示将中断进行中的 AI 调用并隐藏全部 AI 入口，确认后自动保存；开启时若无已启用对话模型则阻止并提示先新建；切换即自动保存 |
| 查看调用统计 | 切换 [调用统计] 标签页 | 按功能类型 / 工作空间 / 日期 / 模型 / 用户五维度聚合的统计表格（调用次数、token 总用量、平均耗时、失败数），支持日期范围筛选与维度切换；按功能维度展示功能中文名 |

- AI 总开关与系统配置项修改即自动保存（800ms 防抖），Embedding 组与对话模型保持手动 [保存]；
- 对话模型列表与 Embedding 组相互独立，可分别保存与测试；
- 独有配置项与高级自定义参数在提交时合并透传，重名以独有配置项控件值为准；
- 表单校验失败时定位到首个错误项并红字提示。

---


