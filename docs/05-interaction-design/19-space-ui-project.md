# 软件测试平台——项目管理

**文档版本**：V1.0  
**日期**：2026-09-23  
**状态**：已发布

---

## 5. 项目管理

### 5.1 项目引导页

**路由**：`/workspace/projects/onboarding`  
**触发条件**：切换空间后，该空间无任何项目。

```
┌──────────────────────────────────────────────────┐
│                                                  │
│              🚀 欢迎来到「电商平台测试」           │
│                                                  │
│         当前工作空间还没有任何项目                  │
│         创建您的第一个项目开始测试管理              │
│                                                  │
│            [创建第一个项目]                        │
│                                                  │
└──────────────────────────────────────────────────┘
```

点击按钮弹出项目创建弹窗，创建成功后跳转至项目列表页。

### 5.2 项目选择页

**路由**：`/workspace/projects/select`  
**触发条件**：切换空间后无个人默认项目但空间有项目。

```
┌──────────────────────────────────────────────────┐
│  选择项目                                        │
├──────────────────────────────────────────────────┤
│  ┌──────────────┐ ┌──────────────┐               │
│  │ ⭐ 核心功能   │ │   用户中心    │               │
│  │ 核心模块测试  │ │ 用户模块测试  │               │
│  │ [设为默认]   │ │ [设为默认]   │               │
│  │ [进入项目]   │ │ [进入项目]   │               │
│  └──────────────┘ └──────────────┘               │
└──────────────────────────────────────────────────┘
```

**交互说明**：
| 操作 | 触发方式 | 反馈 |
|------|----------|------|
| 进入项目 | 点击[进入项目] | 设置活跃项目ID → 跳转至项目工作台 |
| 设为默认 | 点击[设为默认] | 调用接口 → 卡片标注⭐，Toast提示 |

### 5.3 项目列表页

**路由**：`/workspace/projects`（通过顶部动态菜单“项目列表”进入）

#### 5.3.1 页面布局

```
┌──────────────────────────────────────────────────┐
│  项目管理                          [+ 创建项目]   │
├──────────────────────────────────────────────────┤
│  [状态: 全部 ▼] [🔍 搜索项目...]                 │
├──────────────────────────────────────────────────┤
│  ┌──────────────────┐ ┌──────────────────┐       │
│  │ ⭐ 核心功能测试   │ │   用户中心改版   │       │
│  │ 活跃 | 2026-08~09│ │ 活跃 | 2026-07~12│       │
│  │ [设为默认][进入] │ │ [设为默认][进入] │       │
│  │ (管理员:归档 删除)│ │ (管理员:归档 删除)│       │
│  └──────────────────┘ └──────────────────┘       │
├──────────────────────────────────────────────────┤
│                    第1页/共3页                    │
└──────────────────────────────────────────────────┘
```

#### 5.3.2 交互说明

| 操作    | 触发方式              | 反馈                           |
| ----- | ----------------- | ---------------------------- |
| 搜索/筛选 | 输入关键词或选择状态        | 列表自动刷新                       |
| 创建项目  | 点击[创建项目]          | 弹出表单弹窗（名称、描述、起止时间），校验名称空间内唯一 |
| 进入项目  | 点击[进入]或卡片         | 设置活跃项目ID → 跳转至项目工作台          |
| 设为默认  | 点击[设为默认]          | 调用接口 → 卡片标注⭐                 |
| 编辑项目  | 点击[编辑]（管理员或创建者）   | 弹窗预填信息，修改后保存                 |
| 归档    | 点击[归档]（管理员）       | 二次确认 → 检查进行中计划 → 执行          |
| 启封    | 点击[启封]（管理员，已归档项目） | 状态恢复为活跃                      |
| 删除    | 点击[删除]（管理员）       | 二次确认 → 检查项目是否为空 → 执行         |

---


