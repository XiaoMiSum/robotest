# 软件测试平台——系统管理模块页面交互设计总览

**文档版本**：V1.0  
**日期**：2026-09-23  
**状态**：起草中

---

## 1. 引言

### 1.1 编写目的

本文档为系统管理模块页面交互设计的完整文档，为前后端实现提供交互依据，涵盖管理端总体布局与全局交互规范（第 2 章）、数据概览页（第 3 章）、用户管理（第 4 章）、工作空间管理（第 5 章）、角色与权限管理（第 6 章）与通用交互模式总结（第 7 章）。

本版本范围约定：

- **数据概览页**为新版设计，按示例 `web/demos/admin/dashboard.html` 实现，整体替代原「仪表盘」页设计；该页不提供「导出报表」功能（示例中含该按钮，按需求裁剪），KPI 卡片亦不展示示例中的「接口调用 12.4k · 失败 0.3%」脚注，详见 3.2、3.6。
- **用户管理**在既有交互基础上扩展用户状态「**锁定**」：状态取值、展示、行操作与登录限制见 4.2；其余交互（搜索、批量操作、重置密码、分页等）保持不变。
- 工作空间管理、角色与权限管理的页面设计分别见第 5、6 章。

### 1.2 定义与缩写

| 术语 | 定义 |
| ---- | ---- |
| 数据截至 | 统计接口返回的 `generatedAt`（统计生成时间），展示于页面副标题 |
| 登录人次 | 一次成功登录记 1 人次；同一用户当日多次登录累加（用于「今日活跃」KPI） |
| 活跃用户 | 某自然日内按用户去重的登录用户数（用于 14 日趋势图） |
| 锁定 | 用户状态 `locked`，由管理员设置，与「禁用」一样阻止登录 |

---


## 2. 总体布局

### 2.1 管理端整体框架

管理端采用「顶部导航栏 + 左侧分组侧栏 + 内容区」结构（壳层形态见 `docs/05-interaction-design/03-visual-design.md` §6.1，双悬浮白卡方案 B）：

- **顶部导航栏**（固定高度 48px，全平台统一）：左侧为平台 Logo + 「系统管理」模式标签；右侧为全局入口（我的空间（有活动空间时）、系统管理（激活态）、消息中心、用户下拉：修改密码/退出登录）。**平台标识位于顶栏，侧栏不承载 Logo。**
- **左侧分组侧栏**：固定宽度 180px（`--sidebar-width`），浅色悬浮卡片（描边 + 圆角 + 浮影），含分组标题与垂直菜单，等高栏内滚动。
- **内容区**：整幅悬浮白卡，与侧栏卡同描边、同圆角、同浮影；左缘 = 16（页边距）+ 180（侧栏）+ 16（间距）= 212px。
- **侧栏菜单分组**（由路由声明派生，按权限过滤，过滤后为空的分组不渲染）：概览（数据概览，置顶无分组标题）、组织与权限（用户管理、角色管理、空间管理）、平台配置（AI 配置）。

```
┌──────────────────────────────────────────────────────────────┐
│ [⚡RoboTest] [系统管理]          [📁 我的空间] [🔧 系统管理]    │
│                                 [🔔 消息] [👤 张三]           │ ← 顶栏 48px
├────────────┬─────────────────────────────────────────────────┤
│  概览       │  ┌───────────────────────────────────────────┐  │
│   数据概览  │  │                                           │  │
│  组织与权限 │  │           内容区（悬浮白卡）                 │  │
│   用户管理  │  │        （根据路由切换内容）                  │  │
│   角色管理  │  │                                           │  │
│   空间管理  │  └───────────────────────────────────────────┘  │
│  平台配置   │                                                │
│   AI 配置   │                                                │
└────────────┴─────────────────────────────────────────────────┘
   ↑ 侧栏：180px 悬浮白卡，无 Logo；分组标题 10px 大写灰字
```

### 2.2 全局交互规范

- **表格操作**：危险操作（禁用、删除、归档）均需二次确认弹窗。
- **批量操作**：表格支持多选，选中后底部浮现操作栏。
- **消息反馈**：成功/失败操作使用 Toast 提示（顶部居中，3秒自动消失）。
- **表单校验**：前端实时校验并提示错误信息，提交时再次全量校验。
- **加载状态**：数据请求时表格显示骨架屏，按钮显示 loading 状态。

---


## 7. 通用交互模式总结

### 7.1 弹窗类型

| 类型   | 使用场景               |
| ---- | ------------------ |
| 确认弹窗 | 删除、禁用、归档、移除关联等危险操作 |
| 表单弹窗 | 快速创建（如新建工作空间、添加成员） |
| 选择弹窗 | 多选用户关联角色、选择成员      |

### 7.2 加载状态

- **列表加载**：首次显示骨架屏，刷新显示顶部进度条。
- **表单提交**：按钮loading+禁用，完成恢复。
- **树节点操作**：添加/编辑/删除时节点局部loading或全局提示。

### 7.3 响应式处理

- 侧边栏小屏幕下可折叠为图标模式。
- 角色管理左右分栏在小屏幕下切换为上下布局，树组件可折叠。

---

**文档结束**

## 分册-章节对照表

| 分册 | 文件 | 覆盖章节 |
|---|---|---|
| 总览 | `02-system-management-ui-overview.md` | 前言、1. 引言、2. 总体布局、7. 通用交互模式总结 |
| 数据概览 | `03-system-management-ui-dashboard.md` | 3. 数据概览页 |
| 用户管理 | `04-system-management-ui-user.md` | 4. 用户管理 |
| 工作空间管理 | `05-system-management-ui-workspace.md` | 5. 工作空间管理 |
| 角色与权限管理 | `06-system-management-ui-role.md` | 6. 角色与权限管理 |
