# 软件测试平台——（分册：空间与成员管理）

**文档版本**：V1.0
**日期**：2026-09-23
**状态**：已发布

---

### 3.3 空间管理相关接口（/api/workspace）

以下接口操作当前活跃工作空间内的资源，所有接口必须携带请求头 `X-Active-Workspace: <workspace_id>`。

#### 3.3.1 获取当前工作空间详情

- **路径**：`GET /api/workspace`

- **响应**：
  
  ```json
  {
  "id": 1,
  "name": "电商平台测试",
  "description": "负责电商平台相关测试",
  "workspaceRole": "admin",
  "defaultProjectId": 10,
  "defaultProjectName": "核心功能测试",
  "memberCount": 15,
  "projectCount": 5,
  "status": "active",
  "createdAt": "2026-01-01T00:00:00Z"
  }
  ```

#### 3.3.2 更新当前工作空间信息

- **路径**：`PUT /api/workspace`

- **权限**：仅空间管理员（`workspaceRole` 对应预置 `workspace_admin` 角色 ID）可操作。

- **请求体**：
  
  ```json
  {
  "name": "电商平台测试(更新)",
  "description": "新的描述"
  }
  ```

- **校验**：`name` 全局唯一，2~50字符。

- **响应**：更新后的工作空间信息。

#### 3.3.3 设置个人默认项目

- **路径**：`PUT /api/workspace/default-project`
- **请求体**：`{ "projectId": 10 }`，若为 null 则清除个人默认项目。
- **校验**：projectId 若不为 null，必须属于当前活跃工作空间且状态为 active。
- **处理**：更新 workspace_user.default_project_id。
- **响应**：更新后的当前工作空间上下文信息（与 GET /api/workspace 响应结构一致）。

#### 3.3.4 获取成员列表

- **路径**：`GET /api/workspace/members`

- **请求参数**：`keyword`（可选）、`page`、`pageSize`

- **响应**：
  
  ```json
  {
  "records": [
    {
      "userId": 1,
      "username": "zhangsan",
      "email": "zhangsan@example.com",
      "avatarUrl": "...",
      "workspaceRole": "admin",
      "joinedAt": "2026-01-15T10:30:00Z"
    }
  ],
  "total": 8
  }
  ```

#### 3.3.5 直接添加成员

- **路径**：`POST /api/workspace/members`
- **权限**：仅空间管理员可操作。
- **请求体**：`{ "userIds": [3, 4] }`
- **校验**：被邀请用户必须存在且为活跃状态；已在当前工作空间的用户跳过。
- **处理**：批量插入 workspace_user，默认引用预置 `workspace_member` 角色 ID。
- **响应**：`{ "successCount": 1, "skippedUserIds": [4] }`

#### 3.3.6 更新成员角色

- **路径**：`PUT /api/workspace/members/:userId`
- **权限**：仅空间管理员可操作。
- **请求体**：`{ "workspaceRole": "admin" }`
- **校验**：降级操作需检查不能移除最后一位管理员。

#### 3.3.7 移除成员

- **路径**：`DELETE /api/workspace/members/:userId`
- **权限**：空间管理员可移除其他成员；普通成员可自行退出。
- **校验**：不能移除空间最后一位管理员。

#### 3.3.8 创建邀请链接

- **路径**：`POST /api/workspace/invitations`

- **权限**：仅空间管理员可操作。

- **请求体**：
  
  ```json
  {
  "expiresAt": "2026-12-31T23:59:59Z",
  "maxUses": 50
  }
  ```

- **处理**：生成唯一随机 token（64位字符），插入 workspace_invitation 表。

- **响应**：
  
  ```json
  {
  "id": 1,
  "token": "a1b2c3d4e5f6...",
  "expiresAt": "2026-12-31T23:59:59Z",
  "maxUses": 50,
  "useCount": 0,
  "status": "active",
  "createdAt": "2026-07-04T10:00:00Z"
  }
  ```
  
  > 说明：前端根据 token 自行拼接邀请链接 `${baseUrl}/join?token=${token}`。

#### 3.3.9 获取邀请链接列表

- **路径**：`GET /api/workspace/invitations`
- **权限**：空间管理员。
- **响应**：分页返回邀请链接列表。

#### 3.3.10 撤销邀请链接

- **路径**：`PUT /api/workspace/invitations/:id/revoke`
- **权限**：空间管理员。
- **处理**：更新 status='revoked'。

#### 3.3.11 获取项目列表

- **路径**：`GET /api/workspace/projects`

- **请求参数**：`keyword`、`status`、`page`、`pageSize`

- **响应**：
  
  ```json
  {
  "records": [
    {
      "id": 1,
      "name": "用户中心改版",
      "description": "用户中心模块重构测试",
      "status": "active",
      "isDefault": true,
      "startTime": "2026-08-01T00:00:00Z",
      "endTime": "2026-09-01T00:00:00Z",
      "createdBy": { "id": 1, "name": "张三" },
      "createdAt": "2026-07-01T00:00:00Z"
    }
  ],
  "total": 12
  }
  ```

#### 3.3.12 创建项目

- **路径**：`POST /api/workspace/projects`
- **权限**：所有工作空间成员均可创建。
- **请求体**：`{ "name": "...", "description": "...", "startTime": "...", "endTime": "..." }`
- **校验**：name 工作空间内唯一；startTime 不晚于 endTime。
- **响应**：201，返回创建的项目信息。

#### 3.3.13 获取项目详情

- **路径**：`GET /api/workspace/projects/:id`
- **校验**：项目必须属于当前活跃工作空间。

#### 3.3.14 更新项目

- **路径**：`PUT /api/workspace/projects/:id`
- **权限**：空间管理员或项目创建者可操作。
- **校验**：已归档项目不可编辑。

#### 3.3.15 归档/启封项目

- **路径**：`POST /api/workspace/projects/:id/archive`
- **权限**：空间管理员。
- **请求体**：`{ "archived": true }`
- **校验**：归档前检查无进行中的计划。
- **处理**：若归档项目是任一用户的个人默认项目，自动清除。

#### 3.3.16 删除项目

- **路径**：`DELETE /api/workspace/projects/:id`
- **权限**：仅空间管理员。
- **校验**：项目下不能有任何数据。
- **处理**：若删除的项目是任一用户的个人默认项目，自动清除。


### 4.3 项目归档/删除联动

- 项目归档或删除时，系统自动遍历 workspace_user 表，将引用该项目的所有 default_project_id 置为 null。
- 被清除默认项目的用户在项目列表页将看不到默认高亮标识。


### 4.4 邀请成员流程

#### 4.4.1 直接添加流程

管理员在成员管理页面搜索平台已有用户，选择后提交，后端批量插入 workspace_user，默认引用预置 `workspace_member` 角色 ID。

#### 4.4.2 邀请链接流程

1. 管理员创建邀请链接，复制 URL 发送给目标用户。
2. 目标用户访问邀请链接，前端调用验证接口，展示工作空间信息和加入表单。
3. 用户输入邮箱后，前端调用 `POST /api/workspace/invitations/check-email`：
   - 邮箱已注册 → 显示密码输入框，提示用户输入密码验证身份。
   - 邮箱未注册 → 显示姓名和密码输入框，提示用户填写信息创建账号。
4. 用户提交表单后调用 `POST /api/workspace/invitations/join`：
   - 新用户 → 使用 `name`、`email`、`password` 创建用户 → 加入空间 → 生成 Token → 直接登录进入项目列表页。
   - 已有用户 → 验证密码 → 加入空间 → 生成 Token → 直接登录进入项目列表页。
   - 已在空间 → 提示"您已是该工作空间成员"，同时生成 Token 直接登录。
5. 整个流程无需跳转到登录页面，用户提交后即完成加入和登录。


#### 5.3.2 工作空间详情页

**路由**：`/workspace/:workspaceId`

通过顶部动态菜单“空间信息”进入。展示指定工作空间的基本信息，管理员可编辑。

**页面布局**：

```
┌──────────────────────────────────────────────────┐
│  空间信息                                        │
├──────────────────────────────────────────────────┤
│  ┌─── 基本信息 ──────────────────────────┐       │
│  │  名称: [电商平台测试___]  (管理员可编辑)│       │
│  │  描述: [负责电商相关测试_________]    │       │
│  │  成员数: 15    项目数: 5              │       │
│  │  [保存修改] (仅管理员可见)            │       │
│  └──────────────────────────────────────┘       │
└──────────────────────────────────────────────────┘
```

**交互说明**：

| 操作   | 触发方式              | 反馈              |
| ---- | ----------------- | --------------- |
| 编辑信息 | 管理员修改名称/描述 → [保存] | 校验唯一性 → Toast提示 |


#### 5.3.3 成员管理页

**路由**：`/workspace/members`

通过顶部动态菜单“成员管理”进入。操作当前活跃工作空间的成员。

**页面布局**：

```
┌──────────────────────────────────────────────────┐
│  成员管理                                        │
├──────────────────────────────────────────────────┤
│  [👥 成员列表] [🔗 邀请链接]    (管理员可见标签页) │
├──────────────────────────────────────────────────┤
│  ┌─── 成员列表 ──────────────────────────┐       │
│  │  [+ 添加成员] (管理员可见)            │       │
│  │  用户名 │ 角色 │ 加入时间 │ 操作      │       │
│  │ zhangsan│管理员│2026-01-15│[管理员▼][移除]│  │
│  │ lisi    │ 成员 │2026-02-01│[成员▼]  [移除]│  │
│  └──────────────────────────────────────┘       │
└──────────────────────────────────────────────────┘
```

**成员列表标签页交互说明**：

| 操作   | 触发方式              | 反馈                                       |
| ---- | ----------------- | ---------------------------------------- |
| 添加成员 | 点击[添加成员]          | 弹窗：搜索用户（远程搜索，防抖300ms）+ 多选，提交后显示成功数量和跳过列表 |
| 更新角色 | 下拉切换 member/admin | 降级唯一管理员时阻止并提示“至少保留一个管理员”                 |
| 移除成员 | 点击[移除]            | 二次确认弹窗；移除唯一管理员时阻止                        |
| 自行退出 | 点击自己行的[移除]        | “确定退出该工作空间？”→ 执行后切换空间                    |

**邀请链接标签页交互说明**（仅管理员可见）：

| 操作   | 触发方式       | 反馈                                   |
| ---- | ---------- | ------------------------------------ |
| 创建链接 | 点击[创建邀请链接] | 弹窗：设置过期时间、最大次数 → 生成后显示URL + [复制链接]按钮 |
| 复制链接 | 点击[复制链接]   | 一键复制，Toast提示“已复制”                    |
| 撤销链接 | 点击[撤销]     | 二次确认 → 状态变为“已撤销”                     |


#### 5.3.4 项目列表页

**路由**：`/workspace/projects`

通过顶部动态菜单“项目列表”进入。该页面根据工作空间内项目数量自动展示不同内容：空间无项目时显示引导创建区域，有项目时显示项目卡片列表。用户可在此页面设置个人默认项目。

**页面布局（空间有项目时）**：

```
┌──────────────────────────────────────────────────┐
│  项目管理                          [+ 创建项目]   │
├──────────────────────────────────────────────────┤
│  当前工作空间：电商平台测试                       │
│  [状态: 全部 ▼] [🔍 搜索项目...]                 │
├──────────────────────────────────────────────────┤
│  ┌──────────────────┐ ┌──────────────────┐       │
│  │ ⭐ 核心功能测试   │ │   用户中心改版   │       │
│  │ 核心模块重构测试  │ │ 用户模块重构测试  │       │
│  │ 活跃 | 2026-08~09│ │ 活跃 | 2026-07~12│       │
│  │ 创建者: 张三     │ │ 创建者: 李四     │       │
│  │ [设为默认][进入] │ │ [设为默认][进入] │       │
│  │ (管理员:归档 删除)│ │ (管理员:归档 删除)│       │
│  └──────────────────┘ └──────────────────┘       │
│  ┌──────────────────┐                            │
│  │   支付模块(归档)  │                            │
│  │   ...  [启封]    │                            │
│  └──────────────────┘                            │
├──────────────────────────────────────────────────┤
│                < 第1页/共3页 >                    │
└──────────────────────────────────────────────────┘
```

**页面布局（空间无项目时）**：

```
┌──────────────────────────────────────────────────┐
│  项目管理                                        │
├──────────────────────────────────────────────────┤
│                                                  │
│              🚀 欢迎来到「电商平台测试」           │
│                                                  │
│         当前工作空间还没有任何项目                  │
│         创建您的第一个项目开始测试管理              │
│                                                  │
│            [创建第一个项目]                        │
│                                                  │
└──────────────────────────────────────────────────┘
```

**交互说明**：

| 操作    | 触发方式               | 反馈                                                      |
| ----- | ------------------ | ------------------------------------------------------- |
| 搜索/筛选 | 输入关键词或选择状态         | 列表自动刷新                                                  |
| 创建项目  | 点击[创建项目]或[创建第一个项目] | 弹出表单弹窗（名称、描述、起止时间），校验名称空间内唯一                            |
| 进入项目  | 点击[进入]或卡片          | 设置活跃项目ID → 跳转至项目工作台，动态菜单切换为项目模式                         |
| 设为默认  | 点击[设为默认]           | 调用 `PUT /api/workspace/default-project` → 卡片标注⭐，Toast提示 |
| 编辑项目  | 点击[编辑]（管理员或创建者）    | 弹窗预填信息，修改后保存                                            |
| 归档    | 点击[归档]（管理员）        | 二次确认 → 检查进行中计划 → 执行，自动清除引用该项目的默认项目设置                    |
| 启封    | 点击[启封]（管理员，已归档项目）  | 状态恢复为活跃                                                 |
| 删除    | 点击[删除]（管理员）        | 二次确认 → 检查项目是否为空 → 执行，自动清除引用该项目的默认项目设置                   |


