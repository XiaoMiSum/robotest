# 生产环境
# 两种部署方案下前端均与后端同源（合并部署为同一进程；分离部署由 Nginx 反代同域），
# 因此使用相对路径即可，无需硬编码后端域名。
# 如需部署到与后端不同的域名，改为完整地址，例如：
#   VITE_API_BASE_URL=https://api.example.com/api
#   VITE_WS_BASE_URL=wss://api.example.com/ws
VITE_API_BASE_URL=/api
VITE_WS_BASE_URL=/ws
